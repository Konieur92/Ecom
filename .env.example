OPENROUTER=your_openrouter_api_key_here
# ALLOWED_ORIGIN=http://localhost:5173  # override for deployment (set automatically in Docker)
# VITE_API_BASE=http://localhost:3001   # only needed for local dev (not needed in Docker)
