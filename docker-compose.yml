services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - ALLOWED_ORIGIN=*
    volumes:
      - ./output:/app/output
    restart: unless-stopped
